# Claude Project Notes

This folder keeps project-specific Claude context with the repository.

Hidden folders like `.claude` can be skipped by some ZIP/export tools, so this
tracked README makes sure the folder is preserved in GitHub.


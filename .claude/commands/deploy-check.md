# Deploy Check

Before deploying:

1. Run `npm install`.
2. Run `npm start` and open `http://localhost:3000`.
3. Check that room maps load and bookings can be created.
4. Commit the latest files.
5. Push to GitHub.
6. Import the GitHub repository into Vercel.

